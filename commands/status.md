---
description: Show current project state from the changelog
---
Read `CHANGELOG.md` and report only: the `## Current state` block verbatim,
plus the 3 most recent entries. If the file is missing, say so and suggest
`/dev-workflow:init`. Add nothing else.
